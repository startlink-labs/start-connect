{
  "name": "hubspot-oauth-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "set-env:client": "wrangler secret put HUBSPOT_CLIENT_ID",
    "set-env:secret": "wrangler secret put HUBSPOT_CLIENT_SECRET"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250115.0",
    "wrangler": "^4.54.0"
  }
}
